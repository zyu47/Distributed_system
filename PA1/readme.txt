1 ./src/ contains all the source files, including two packages hw1 (major classes) and dep (dependencies)
2. server_list.txt contains the server addresses with the following format: 
	ipAddress:port:machine_name
3. collate_addr.txt contains the collator process address
4. 
